import { GenerationFlow } from "@/components/generate/generation-flow"

export const metadata = {
  title: "Generate a Website",
  description: "Enter a business name and location to generate a complete, modern website in 6 minutes.",
}

export default function CreatePage() {
  return <GenerationFlow />
}
